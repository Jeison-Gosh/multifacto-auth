export const enum TIME_UNITS {
    MILLISECONDS = 'milliseconds',
    SECONDS = 'seconds',
    MINUTES = 'minutes',
    HOURS = 'hours',
    DAYS = 'days',
    MONTHS = 'months',
    YEARS = 'years'
}

export const enum SCHEDULE_OPTION {
    INTERVAL = 'interval',
    CRON = 'cron',
    TIMEOUT = 'timeout'
}